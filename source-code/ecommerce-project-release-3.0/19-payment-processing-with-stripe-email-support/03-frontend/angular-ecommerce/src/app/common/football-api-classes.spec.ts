import { FootballApiClasses } from './football-api-classes';

describe('FootballApiClasses', () => {
  it('should create an instance', () => {
    expect(new FootballApiClasses()).toBeTruthy();
  });
});
